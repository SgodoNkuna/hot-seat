// Update this once the server is deployed (see server/render.yaml) — e.g. 'wss://hot-seat-server.onrender.com'.
// ponytail: single constant, no env/config-file machinery until multiple environments actually exist.
export const DEFAULT_SERVER_URL = 'ws://localhost:4000';
